---
title: Writings
description: My thoughts, ideas and experiences with the things I find interesting
---

<div class="grid grid-cols-2">
{% for tag in collections.all | getAllTags | filterTagList %}
  {% set postslist = collections[ tag ] %}
  <div class="flex flex-col">
    <h2 class="mt-12 text-2xl font-bold text-neutral-700 first:mt-8 dark:text-neutral-300">{{ tag }}</h2>
    <hr class="border-dotted w-36 border-neutral-400 mb-6">
    <div class="flex flex-col items-stretch gap-4">
      {% for post in postslist %}
        <article class="grid grid-cols-6 gap-4 max-w-prose">
          {# Imagen #}
          <div class="col-span-2">
            <a class="flex" href="{{ post.url }}">
              <img class="w-24 rounded-md sm:w-40" src="/images/thumbnails/{{ post.data.thumbnail }}">
            </a>
          </div>
          {# Descripcion #}
          <div class="col-span-4">
            <h3 class="flex items-center text-xl font-semibold">
              <a class="text-neutral-800 decoration-primary-500 hover:underline hover:underline-offset-2 dark:text-neutral" href="{{ post.url }}">{{ post.data.title }}</a>
            </h3>
            <div class="text-sm text-neutral-500 dark:text-neutral-400">
              <div class="flex flex-row flex-wrap items-center">
                <time datetime="{{ post.date | ISODate }}">{{ post.date | date("D MMMM YYYY") }}</time>
                <span class="px-2 text-primary-500">·</span>
                <span>??? words</span>
                <span class="px-2 text-primary-500">·</span>
                <span title="Reading time">{{ post.content | timeToRead }}</span>
              </div>
            </div>
          </div>
        </article>
      {% endfor %}
    </div>
  </div>
{% endfor %}
</div>
