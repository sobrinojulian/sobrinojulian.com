<article class="flex flex-row max-w-prose items-center p-6">
  {# Imagen #}
  <div class="flex-none pr-4 sm:pr-6">
    <a href="{{ post.url }}">
      <img class="w-24 rounded-md sm:w-28" src="/images/thumbnails/{{ post.data.thumbnail }}">
    </a>
  </div>
  {# Descripcion #}
  <div class="flex flex-col text-sm text-neutral-500 dark:text-neutral-400">
    {# Titulo #}
    <h3 class="flex items-center text-xl font-semibold">
      <a class="text-neutral-800 decoration-primary-500 hover:underline hover:underline-offset-2 dark:text-neutral"
        href="{{ post.url }}">{{ post.data.title }}</a>
    </h3>
    {# Metadata #}
    <div class="flex flex-row flex-wrap items-center">
      <time datetime="{{ post.date | ISODate }}">{{ post.date | date("D MMMM YYYY") }}</time>
      <span class="px-2 text-primary-500">·</span>
      <span title="Reading time">{{ post.content | timeToRead }}</span>
    </div>
  </div>
  </div>
</article>
